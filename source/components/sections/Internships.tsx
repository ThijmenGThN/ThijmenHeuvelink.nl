import type Directus from '@/types/Directus'

export default function Internships({ API }: { API: Directus }) {

    return (
        <div className='mx-auto container my-8'>
            Internships.
        </div>
    )
}
